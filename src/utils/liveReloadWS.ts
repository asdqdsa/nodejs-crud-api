// import { config } from './config.ts'
// import { WebSocketServer } from 'ws'

// export const liveReload = (port = config.port) => {
//   const wss = new WebSocketServer({ port })
//   wss.on('connection', (ws: WebSocket) => {
//     ws.send('connected')
//   })

//   return wss
// }
